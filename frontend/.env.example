# Where the dev server proxies /api. The app itself never uses an absolute API
# URL: it always calls same-origin /api/v1/..., because the refresh cookie is
# first-party and /auth/refresh verifies the request Origin.
VITE_API_TARGET=http://127.0.0.1:8000
