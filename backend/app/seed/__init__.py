"""Demo data for the deployed application.

Split in two: ``data`` is the studio (names, timetable, membership dates) and
``runner`` is the procedure that brings it into being. The split is so the
procedure can be read without wading through content, and so the studio can be
re-imagined without touching anything that talks to the database.
"""
